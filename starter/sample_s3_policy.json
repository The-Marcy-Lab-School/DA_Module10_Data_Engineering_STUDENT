{
  "_comment": "GIVEN, real, deliberately flawed -- a sample bucket policy for a bucket that stores this pipeline's Delta/Parquet output. Real security review exercise: find the real over-permissive setting and propose a real fix in required_components.md Section 4.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowPipelineReadWrite",
      "Effect": "Allow",
      "Principal": "*",
      "Action": [
        "s3:GetObject",
        "s3:PutObject",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::m9-pipeline-output",
        "arn:aws:s3:::m9-pipeline-output/*"
      ]
    }
  ]
}
